services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: loanrepay
    restart: unless-stopped
    volumes:
      - ../data:/app/data
    ports:
      - "127.0.0.1:5050:8000"
    environment:
      - DATA_DIR=/app/data
